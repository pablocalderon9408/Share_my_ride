"""Rides app."""

# Django
from django.apps import AppConfig


class RidesAppConfig(AppConfig):
    """users app config."""

    name = "cride.rides"
    verbose_name = "Rides"
    verbose_name_plural = "Rides"
